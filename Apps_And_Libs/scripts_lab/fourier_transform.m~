load im_cube_class.mat

for i=1:length(im_cube_class.images)
    image = im_cube_class.images(:,:,i);
    four_res = fft(image);
    
    magnitudeY = log(abs(four_res)+1);
    phase = unrap(angle(four_res));
    fft_instance = FFT
end